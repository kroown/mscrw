[package]
name = "mscrw"
version = "1.0.0"
edition = "2021"
description = "metadata scraper for windows"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
walkdir = "2"

[profile.release]
opt-level = "z"
lto = true
strip = true
codegen-units = 1
